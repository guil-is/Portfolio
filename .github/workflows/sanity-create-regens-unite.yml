name: Sanity — Create Regens Unite project

on:
  push:
    branches: [main]
    paths: [".github/triggers/create-regens-unite.json"]

jobs:
  create:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
      - run: npm ci
      - name: Resolve inputs
        id: cfg
        run: |
          echo "DRY_RUN=$(jq -r 'if has("dry_run") then .dry_run else true end | tostring' .github/triggers/create-regens-unite.json)" >> "$GITHUB_OUTPUT"
      - name: Create Regens Unite
        env:
          SANITY_PROJECT_ID: ${{ secrets.SANITY_PROJECT_ID }}
          SANITY_DATASET: ${{ secrets.SANITY_DATASET }}
          SANITY_AUTH_TOKEN: ${{ secrets.SANITY_AUTH_TOKEN }}
          DRY_RUN: ${{ steps.cfg.outputs.DRY_RUN }}
        run: npx tsx scripts/sanity/create-regens-unite.ts
