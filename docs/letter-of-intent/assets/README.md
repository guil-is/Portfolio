# Letter of intent — assets

Drop the two brand assets here so the letterhead can reference them:

- `logo.png` — the guil.is logo mark for the top letterhead.
- `signature.png` — the handwritten signature for above the signature block.

Both are consumed by the letter build; they are not shipped as part of the
public site (kept out of `public/`).
